# Agent 工作流

本文规定本仓库中 Agent 制定编程训练题时必须执行的流程。

每次制定题目都必须将完整题面写入目标实验的 `README.md`，明确接口、行为规则和验收场景。实现代码与要求中的单元测试由练习者手写。

## 1. 确定目标实验

收到制定或生成题目的请求后，先阅读 `docs/plan.md`，并结合用户明确指定的实验编号确定唯一目标目录：

```text
experiment/experimentN/
├── README.md
└── code/
```

必须遵守：

- 题面只写入 `experiment/experimentN/README.md`；
- `code/` 属于练习者，不得在出题阶段替练习者编写或修改答案；
- 不得覆盖其他实验；如果目标实验无法唯一确定，必须先向用户确认。

目标目录不存在时，可以创建该实验目录及空的 `code` 目录。

## 2. 制定题面

写题目前必须完整阅读 `docs/problem-make.md`，并严格按照其中的范式、测试规则和发布检查清单编写题面，不能只参考已有实验的格式。

题面至少需要明确：

- 元信息、训练目标、基础范围、非目标和可选加分项；
- 实现目录、必交文件、包名、最低语言版本和依赖策略；
- Go module 的准确位置、唯一工作目录及格式化、编译和测试命令；
- 精确的公共 API，或 CLI、文件、网络协议等输入输出契约；
- 合法输入、错误处理、边界条件、状态变化和副作用；
- 使用 `REQ-*` 编号的可观察基础行为；
- 使用 `CHECK-*` 编号的复杂度、实现限制和人工检查项；
- 至少一个与规则完全一致的完整示例；
- 使用 `TC-*` 编号的测试矩阵，其中每个基础 `REQ-*` 至少对应一个必测场景；
- 可以明确判断的完成条件。

题面必须描述“什么行为是正确的”，但不得包含完整实现、可直接翻译的伪代码、非必要的核心数据结构组合或参考答案。难度应来自问题本身，而不是题意含糊。

## 3. 题面可验收性复核

题面交付前，必须检查：

- API、示例、`REQ-*`、`TC-*` 和完成条件相互一致；
- 所有影响结果的边界、错误和副作用均已定义；
- 对本次新制定或修订的题面，module、包名、目录和运行命令已由题面唯一固定；
- 基础要求与加分项已经完全分开；
- 每个必测场景都能直接追溯到公开题面；
- 复杂度或禁止使用某组件等要求有合适的 Review 或静态检查方式。

如果发现题面冲突或缺失，应先修订目标实验的 `README.md`。不得根据当前实现的行为补充或放宽题目规则。

## 4. 验证与交付

完成题面后，必须：

1. 按 `docs/problem-make.md` 的发布清单复核题面；
2. 复核格式化、编译和测试命令的工具链前置条件、工作目录和路径；
3. 确认没有修改 `code/`、题面之外的用户文件或仓库元数据；
4. 报告修改文件、题面摘要、测试场景、人工检查项、运行命令和实际验证情况。

练习者尚未完成代码时，不得声称实现或测试已经通过。

## 5. 必须停止或修订的情况

遇到以下情况时不得自行猜测：

- 题面、示例、接口或测试矩阵相互冲突；
- module、包名、工作目录或公共接口不足以明确实现和验收方式；
- 只有读取私有字段、绑定某种实现或增加未公开要求才能判断；
- 所需工具链或运行环境不可用。

能够在本次出题范围内消除的题面问题，应先修订题面再继续；不能消除时，报告具体阻塞，不得将环境或题面问题归咎于练习者的实现。
