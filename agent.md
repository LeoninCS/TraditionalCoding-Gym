# Agent 工作流

本文规定本仓库中 Agent 制定编程训练题时必须执行的流程。

每次制定题目都必须同时完成两项交付：

1. 将完整题面写入目标实验的 `README.md`；
2. 在同一实验的 `judge` 目录中编写能够判断基础要求对错的评测机。

不能只完成题面就结束任务，也不能用评测机代替清晰的题面契约。

## 1. 确定目标实验

收到制定或生成题目的请求后，先阅读 `docs/plan.md`，并结合用户明确指定的实验编号确定唯一目标目录：

```text
experiment/experimentN/
├── README.md
├── code/
└── judge/
```

必须遵守：

- 题面只写入 `experiment/experimentN/README.md`；
- 评测机只写入 `experiment/experimentN/judge/`；
- `code/` 属于练习者，不得在出题阶段替练习者编写或修改答案；
- 不得覆盖其他实验；如果目标实验无法唯一确定，必须先向用户确认。

目标目录不存在时，可以创建该实验目录及空的 `code`、`judge` 目录。

## 2. 制定题面

写题目前必须完整阅读 `docs/problem-make.md`，并严格按照其中的范式、测试规则和发布检查清单编写题面，不能只参考已有实验的格式。

题面至少需要明确：

- 元信息、训练目标、基础范围、非目标和可选加分项；
- 实现目录、必交文件、包名、最低语言版本和依赖策略；
- Go module 的准确位置、唯一工作目录及格式化、编译和测试命令；
- 精确的公共 API，或 CLI、文件、网络协议等输入输出契约；
- 合法输入、错误处理、边界条件、状态变化和副作用；
- 使用 `REQ-*` 编号的可观察基础行为；
- 使用 `CHECK-*` 编号的复杂度、实现限制和人工检查项；
- 至少一个与规则完全一致的完整示例；
- 使用 `TC-*` 编号的测试矩阵，其中每个基础 `REQ-*` 至少对应一个必测场景；
- 可以明确判断的完成条件。

题面必须描述“什么行为是正确的”，但不得包含完整实现、可直接翻译的伪代码、非必要的核心数据结构组合或参考答案。难度应来自问题本身，而不是题意含糊。

## 3. 题面可评测性复核

开始编写评测机前，必须检查：

- API、示例、`REQ-*`、`TC-*` 和完成条件相互一致；
- 所有影响结果的边界、错误和副作用均已定义；
- 对本次新制定或修订的题面，module、包名、目录和运行命令已由题面唯一固定；仅审计规范建立前的旧题时，才可以按照 `docs/judge-make.md` 的兼容规则识别题面已允许的多种布局；
- 基础要求与加分项已经完全分开；
- 每个自动测试都能直接追溯到公开题面；
- 复杂度或禁止使用某组件等要求有合适的 Review 或静态检查方式。

如果发现题面冲突或缺失，应先返回题面阶段修订目标实验的 `README.md`，再继续生成评测机。不得让 judge 猜测规则或根据当前候选实现补充答案。

## 4. 编写评测机

题面定稿后，必须完整阅读 `docs/judge-make.md`，优先使用其中的“提示词一：首次生成评测机”。已有 judge 时，根据任务使用审计、回归测试或误判修复提示词。

评测机的持久化修改只能位于目标实验的 `judge` 目录，至少包括：

```text
judge/
├── run.sh
├── README.md
└── 评测代码及按需创建的 testdata/
```

评测机必须：

- 以实验目录下执行 `bash judge/run.sh` 作为唯一入口；
- 通过公开 API、输入输出、退出码和公开副作用进行黑盒评测；
- 覆盖题面声明的基础 `REQ-*` 与必测 `TC-*`；
- 将每个 `CHECK-*` 按题面声明映射到可靠的静态检查或人工 Review；无法可靠自动验证的项目必须列为待人工检查，不得影响自动 `PASS/FAIL`；
- 不测试加分项、非目标、契约外输入或私有实现；
- 不读取私有字段，不依赖特定字段名、内部结构或某一种正确算法；
- 保持测试确定、独立和可复现，固定随机种子并清理临时资源；
- 不依赖真实网络、真实时间、测试顺序、用户全局配置或紧迫的耗时阈值；
- 不修改或反写 `code/`，不持久化参考实现，也不把当前实现当作期望结果来源；
- 在 `judge/README.md` 中记录题面路径、运行前置条件、覆盖矩阵和待人工检查项。

统一 verdict 契约：

| 退出码 | stdout 最后一个非空行 | 含义 |
| --- | --- | --- |
| `0` | `VERDICT: PASS` | 所有可自动评测的基础要求通过 |
| `1` | `VERDICT: FAIL` | 候选代码缺失、编译失败、测试失败、超时或违反可自动检查的题面要求 |
| `2` | `VERDICT: JUDGE_ERROR` | 题面冲突、工具链缺失、judge 故障或无法公平判定 |

详细诊断必须写入 stderr。退出码与 stdout 最后一个非空行的 `VERDICT` 缺失或不一致时，本次结果无效，按 `JUDGE_ERROR` 报告。

`PASS` 只表示自动评测通过；仍待人工确认的 `CHECK-*` 必须明确列出，不能宣称整个实现已被完全证明正确。

## 5. 验证与交付

完成题面和评测机后，必须：

1. 按 `docs/problem-make.md` 的发布清单复核题面；
2. 格式化评测代码并运行 judge 自检或静态检查；
3. 从目标实验目录实际执行 `bash judge/run.sh`；
4. 确认退出码与 stdout 最后一行的 `VERDICT` 一致；
5. 确认没有修改 `code/`、题面之外的用户文件或仓库元数据；
6. 报告修改文件、题面摘要、覆盖矩阵、未自动评测项、运行命令和实际结果。

如果候选代码尚未完成，runner 返回 `FAIL` 可以证明入口正常工作，但只能报告“评测机已生成，当前候选实现未通过”，不得声称题目实现已经完成。

## 6. 必须停止或降级的情况

遇到以下情况时不得自行猜测：

- 题面、示例、接口或测试矩阵相互冲突；
- module、包名、工作目录或公共接口不足以建立公平评测；
- 只有读取私有字段、绑定某种实现或增加未公开要求才能判断；
- 工具链、隔离设施或 judge 自身发生故障。

能够在本次出题范围内消除的题面问题，应先修订题面再继续；不能消除时，报告具体阻塞并使用 `JUDGE_ERROR`，不能将其记为候选实现的 `FAIL`。
